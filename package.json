{
  "name": "fixei",
  "version": "1.0.0",
  "description": "Autonomous bug-fixing pipeline — from ticket to production deploy, zero human intervention.",
  "author": "Daniel Plácido",
  "license": "MIT",
  "type": "module",
  "main": "src/api/server.js",
  "scripts": {
    "start": "node src/api/server.js",
    "dev": "node --inspect --watch src/api/server.js",
    "test": "node --experimental-vm-modules node_modules/.bin/jest --verbose --coverage"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2",
    "dotenv": "^17.3.1",
    "express": "^4.18.2",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}